# Sample Calculator Repository

This is a tiny repository used by MiMo Code Surgeon to demonstrate:

```text
Issue -> test generation -> test failure -> code repair -> test pass -> PR description
```

The intentionally unclear behavior is `calculator.divide(a, b)` when `b == 0`.
